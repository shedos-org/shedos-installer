# ShedOS Locale Module Configuration

# Region and zone for timezone
region: "America"
zone: "New_York"

# Locale generation settings
localeGenPath: /etc/locale.gen

# GeoIP for automatic location detection
geoip:
    style: "json"
    url: "https://ipapi.co/json"
    selector: "timezone"
