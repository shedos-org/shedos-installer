# Copy /etc/NetworkManager/system-connections/<ssid>.nmconnection from
# the live root to /target so the user's Wi-Fi pick during install
# auto-connects on first boot of the installed system. ShedOS doesn't
# use netctl; pin both flags explicitly so future Calamares default-
# changes can't silently regress this.
---
networkmanager: true
netctl: false
