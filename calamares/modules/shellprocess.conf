# ShedOS shellprocess override
# This overrides the default Calamares example config that contains "slowloris"
# We use shellprocess instances (shellprocess@bootloader) for actual commands

dontChroot: false
timeout: 10
script:
    - "/usr/bin/true"
