# ShedOS unpackfs configuration
# Extracts the squashfs filesystem to the target

unpack:
    - source: "/run/archiso/airootfs"
      sourcefs: "squashfs"
      destination: ""
      excludeFile: "/etc/calamares/modules/unpackfs-exclude.conf"
