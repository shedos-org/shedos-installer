# ShedOS initcpio configuration
# Runs mkinitcpio to generate initramfs for installed system

# Use safe defaults
safe: true

# Kernel to generate initramfs for. shedos-kernel is the only kernel
# ShedOS ships; vanilla `linux` was dropped from base.txt.
kernel: shedos-kernel
