# ShedOS Welcome Module Configuration

showSupportUrl: true
showKnownIssuesUrl: true
showReleaseNotesUrl: true
showDonateUrl: false

# System requirements
requirements:
    requiredStorage: 20.0      # GB minimum
    # GiB available after kernel reservations (firmware + iGPU eat
    # 0.3-2.0 GB on shared-memory laptops). 6.0 accepts every 8 GB
    # stick; 4 GB systems still fail.
    requiredRam: 6.0
    check:
        - storage
        - ram
        - root
    required:
        - ram

# GeoIP autodetection is intentionally not configured. The install is
# fully offline; a third-party probe would either fail (no network) or
# leak the user's IP for no benefit. Users pick their timezone manually.

# Language selection
languageIcon: languages
