# Orphaned: settings.conf no longer runs this module. Calamares' DMgreetd
# rewrites default_session.command to a fallback greeter (agreety/tuigreet/
# cage+gtkgreet) which would clobber our Hyprland-as-greeter command, so
# shedos_finalize handles greetd autologin via tomlkit instead. Kept here
# for reference if anyone re-enables the module.

displaymanagers:
  - greetd

basicSetup: false
sysconfigSetup: false
