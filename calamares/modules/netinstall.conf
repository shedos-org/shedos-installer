# ShedOS optional-apps picker — uses Calamares' built-in `netinstall`
# module for a checkbox tree view. Reads its group + package tree from
# netinstall.yaml in the same dir.
---
groupsUrl: file:///etc/calamares/modules/netinstall.yaml
required: false
label:
    sidebar: "Optional Apps"
    title:   "Optional Apps"
